data_loader.py
strategy_gate.py 통과안되서  strategy_spec.yaml를 고치라고 햇고 종목이 두개라 염두해두어야 하고 
지금이거는 아침에 300개 종목을 찾아서 실전진행하기전 종목 찾는내용임 

대표님, 맞습니다. 지금처럼 여러 고비를 넘다 보면 자칫 **'어디까지 고쳤고, 어디가 구멍인지'** 놓치기 쉽죠. **INTP**답게 전체 지도를 펼쳐놓고 **'현재 중단 지점'**과 **'최종 합격 요건'**을 박제로 만들어 드릴게요.

이 내용을 메모장에 복사해 두시면, 나중에 전략 조건식을 완성했을 때 삽질 없이 바로 **PASS**를 받아내실 수 있습니다. [cite: 2026-03-28]

---

### 📍 1. 현재 상황: 어디까지 왔고, 왜 중단되었나?

우리는 **'데이터 공급망(V30)'**의 4개 부품 중 2개를 완벽히 고쳤고, 2개는 **'전략 미완성'** 사유로 보류된 상태입니다. [cite: 2026-03-28]

| 부품명 | 상태 | 중단 사유 및 잔존 리스크 |
| :--- | :--- | :--- |
| **data_rules.yaml** | **PASS** ✅ | 50개 제한 및 자산 타입 정의 완료. [cite: 2026-03-28] |
| **data_loader.py** | **PASS** ✅ | 절대경로, fsync, 공집합 에러 처리 완비. [cite: 2026-03-28] |
| **strategy_spec.yaml** | **FAIL** ❌ | **`entry_condition`** 키(칸) 자체가 없어서 연계 불가. [cite: 2026-03-28] |
| **strategy_gate.py** | **FAIL** ❌ | YAML 키 경로 오류 및 **`run.py`**에 임포트 안 됨. [cite: 2026-03-28] |

---

### 🛠️ 2. 앞으로 '통합 패스'를 위해 수정할 핵심 로직

나중에 전략을 완성하실 때, 아래 **3가지 포인트**만 딱 지켜서 코드를 짜시면 클로드도 두말없이 통과시킬 겁니다. [cite: 2026-03-28]

#### ① YAML에 '칸' 만들기 (SSOT 확보)
`strategy_spec.yaml`에 반드시 아래 계층 구조를 만드세요. [cite: 2026-03-17]
```yaml
strategy:
  universe: { top_n: 50 }
  entry_condition: { min_change_rate: 3.0 } # 이 칸이 있어야 코드가 작동함
```

#### ② 하드코딩 제거 (Path Integrity)
코드 안에 `C:\Users\...` 같은 경로를 절대 쓰지 마세요. [cite: 2026-03-28]
* **해결**: `os.path.join(strategy_path, "파일명")`을 써서 엔진이 주는 경로를 그대로 받아 쓰세요. [cite: 2026-03-17]

#### ③ 오케스트레이터 연결 (Integration)
`run.py`가 낡은 부품(`selector.py`)이 아니라 새 부품(`strategy_gate.py`)을 바라보게 갈아 끼워야 합니다. [cite: 2026-03-28]

---

### 📝 3. 클로드에게 던질 '치트키' (최종 통과용 로직 내용)

전략이 완성된 날, 아래 로직을 복사해서 클로드에게 **"이대로 고쳐줘"**라고 하시면 됩니다. [cite: 2026-03-28]

```python
# strategy_gate.py 에 들어갈 표준 규격 [cite: 2026-03-28]
def filter_by_strategy(market_data, strategy_path):
    # 1. YAML 로드: spec['strategy']['entry_condition']['min_change_rate'] 추출
    # 2. 데이터 필터: market_data(리스트) -> DataFrame 변환
    # 3. 1차 필터: 거래대금 상위(top_n) 추출
    # 4. 2차 필터: 등락률(min_change_rate) 이상인 것만 반환
    # 5. 예외 처리: 데이터 없으면 ValueError 발생시켜 조용한 실패 차단
```



---

### 🚀 대표님의 다음 스텝 (Next Action)

지금은 **`data_loader.py`**가 300개를 가져와서 엔진에 쏴주는 것까지만 확인하시고 푹 쉬세요. [cite: 2026-03-28]

나중에 **"이제 조건식 다 짰어, 합체하자!"**라고 저를 부르시면, 제가 위에서 정리한 **3단계 수술**을 1초 만에 집도해 드릴게요.

**이 지도를 메모장에 저장해 두시겠습니까?** 아니면 내일 아침 **데이터 흐름 테스트**를 위해 제가 `run.py` 수정본만 따로 뽑아드릴까요?
 [cite: 2026-03-28] 무엇부터 할까요?
 
 감사를 시작한다.---

## 감사 판정

---

### 1. `data_loader.py`

```
PASS (이전 FAIL·RISK 3건 해소 확인)
- valid_types 공집합 → ValueError raise 확인
- __file__ 기준 경로 → strategy_path 기준 통합 확인
- FileNotFoundError raise → 조용한 실패 제거 확인
```

---

### 2. `strategy_gate.py`

```
FAIL:
- entry_condition 키가 strategy_spec.yaml에 존재하지 않음
  → entry_cond = {} → min_change = 3.0 (하드코딩 기본값 고정)
  → "YAML에서 5.0으로 바꾸면 즉시 반영" 주석은 사실과 다름
  → 실질적으로 코드 내 하드코딩과 동일한 동작 유지
  → 이전 회차 동일 RISK 미해소
```

---

### 3. 연결 구조 통합 판정

```
FAIL:
- run.py가 strategy_gate.filter_by_strategy를 호출하지 않음
  → 이전 회차 지적 이후 run.py 미수정
  → filter_by_strategy는 실전 파이프라인 외부에 존재
  → 사문화된 모듈로 판정
```

---

### 최종 결론

**결격 사유 잔존: YES**

`data_loader.py` 단독 PASS. `strategy_gate.py`의 `min_change_rate` YAML 연계는
 **3회 연속 미해소** — `strategy_spec.yaml`에 `entry_condition` 키를 추가하지 않는 한 구조적으로 해결 불가. `run.py` 
편입은 본 감사 범위 외 수정 사항이나 미조치 상태. **결격 사유 2건 잔존**.