

## ① LOCKED (동결 영역) `LOCKED/`

LOCKED/
├─ [01] CONSTITUTION/
│   ├─ * system_config.yaml              # SSOT: 안전 상한선(수치/정책)만
│   ├─ * schema_system_config.json       # system_config 스키마(2020-12)
│
├─ [02] GOVERNANCE/
│   ├─ * stage_contracts.yaml            # S0~S11 단방향 계약(역행 금지)
│   ├─ * schema_stage_contracts.json     # stage_contracts 스키마
│
├─ [03] AIP/
│   ├─ * addendum_aip.yaml               # 감사 기준(PASS/FAIL/RISK)
│   └─ * schema_addendum_aip.json        # addendum_aip 스키마
│
├─ [04] POLICY/
│   ├─ * worker_allowlist.yaml           # run.py 외 실행 금지
│   └─ * recovery_policy.yaml            # SAFE_HALT 후 자동복구 금지(Fail-Secure)
│
└─ [05] GUARDS/
    ├─ * preflight_gate.py               # 실행 전: 필수 파일 존재 확인
    └─ * integrity_guard.py              # system_config 변조 즉시 SAFE_HALT


# ② RUNTIME (실행 엔진) `RUNTIME/`

RUNTIME/
├─ *[06] run.py                          # 유일 진입점 (오늘 Claude PASS)
├─ [07] scheduler.py                     # 시간 정보만 제공
├─ [08] market_data.py                   # 데이터 입력
├─ [09] regime_filter.py                 # 레짐 판별
├─ [10] entry_engine.py                  # 진입 신호 생성
├─ [11] risk_gate.py                     # SSOT 상한 검사
├─ [12] position_sizer.py                # 비중/수량 산정
├─ *[13] order_intent.py                 # 주문 의도 생성 (오늘 PASS)
├─ *[14] pre_order_check.py              # 주문 전 정책 검사 (오늘 PASS)
├─ *[15] order_manager.py                # 주문 실행 (오늘 PASS)
├─ *[16] fill_tracker.py                 # 체결 추적 (오늘 PASS)
├─ *[17] position_reconciler.py          # 포지션 동기화 (오늘 PASS)
└─ [18] exit_engine.py                   # 청산 집행


# ③ EVIDENCE (불변 증거) `EVIDENCE/`

EVIDENCE/
├─ [19] ledger_writer.py                 # append-only 거래 원장
├─ [20] audit_writer.py                  # 감사 기록
├─ [21] incident_writer.py               # SAFE_HALT 사고 기록
└─ [22] STATE/
    └─ state.json                        # 실시간 상태 저장


# ④ STRATEGY (유일 변경 영역) `STRATEGY/`

STRATEGY/
├─ [23] strategy_schema.json             # strategy_spec 스키마
├─ *[24] strategy_spec.yaml              # 전략 정의
├─ [25] change_log.md                    # 전략 변경 기록
└─ [26] scheduler_config.yaml            # (전략 설계 후 생성)


# ⑤ n8n (신경계)

n8n/
└─ Trigger → RUNTIME/run.py 호출
